# 📚 ZariYo 학습 노트 - 2026년 7월 13일 (월)

본 문서는 2026년 7월 13일 진행된 Spring Boot 계층형 아키텍처, Redisson 분산 락 동시성 제어, Swagger (OpenAPI 3) API 명세화 및 전역 예외 처리기 학습 기록입니다.

---

### [2026-07-13 13:40] Q8. Spring Boot API의 아키텍처 흐름 (Controller - Service - Repository - DB)
**질문 (User)**:
"백엔드 API를 구축할 때 패키지가 여러 개로 쪼개지고 파일이 많아지는데, 전체적인 데이터 흐름과 각 역할이 어떻게 되는지 알고 싶어."

**답변 (Antigravity)**:
Spring Boot 백엔드 개발에서는 유지보수와 역할의 분리를 위해 **계층형 아키텍처(Layered Architecture)**를 사용합니다. 프론트엔드에서 API 요청을 보낸 후 DB를 거쳐 다시 응답을 받기까지 데이터는 순서대로 통과합니다.

#### 1. 각 레이어의 핵심 역할과 주석
* **Controller (컨트롤러)**: 프론트엔드의 요청을 받는 **문지기**입니다. 주소와 HTTP Method를 매핑하고, 들어온 JSON 데이터를 자바 객체(DTO)로 변환해 서비스에 넘겨줍니다. (`@RestController`, `@PostMapping` 등)
* **Service (서비스)**: 핵심 비즈니스 로직을 수행하는 **두뇌**입니다. "이미 예약된 좌석인가?" 같은 논리적 흐름을 판단하고, `@Transactional`을 붙여 DB 작업들의 원자성을 관리합니다.
* **Repository (리포지토리)**: 데이터베이스에 직접 접근하는 **수행 비서**입니다. JPA의 `JpaRepository` 인터페이스를 상속받아 SQL 작성 없이 조회, 저장, 삭제 메서드를 무료로 사용합니다.
* **DTO (Data Transfer Object)**: 레이어 간 이동할 때 담기는 **택배 상자**입니다. DB 테이블 모양인 Entity를 그대로 노출하지 않고 딱 필요한 데이터만 담아 통신합니다.

---

### [2026-07-13 13:45] Q9. Redisson 분산 락(Distributed Lock)을 이용한 동시성 제어 원리
**질문 (User)**:
"자리요의 핵심인 동시성 제어를 위해 Redisson 분산 락을 썼는데, 이게 무엇이고 어떤 원리로 여러 명의 중복 예약을 막는 거야? tryLock의 동작 방식도 궁금해."

**답변 (Antigravity)**:
동시성 제어는 **"동일한 좌석에 두 명 이상의 유저가 0.001초 차이로 동시에 클릭했을 때, 예약이 중복되는 것을 막는 기술"**입니다.

#### 1. 왜 분산 락이 필요한가?
자바 언어 자체의 기본 동기화 기능(`synchronized`)은 개별 서버 메모리 안에서만 동작하므로 여러 서버에서 들어오는 요청을 제어하지 못합니다. 따라서 외부 메모리 저장소인 **Redis**를 중재자로 활용하여 **"이 좌석은 내가 먼저 찜했으니 다른 서버들은 대기하라"**고 잠금장치를 거는 것이 **분산 락(Distributed Lock)**입니다.

#### 2. Redisson 분산 락의 핵심 동작 방식 (`tryLock`)
```java
RLock lock = redissonClient.getLock("lock:seat:" + seatId);
acquired = lock.tryLock(5, 10, TimeUnit.SECONDS);
```
* **`"lock:seat:" + seatId`**: 좌석 고유 ID를 기준으로 락 이름을 지정하여 다른 좌석 사용자들이 대기하지 않고 독립적으로 동작하도록 만듭니다.
* **`tryLock(5, 10, TimeUnit.SECONDS)`**:
  - **Wait Time (5초)**: 락 획득 실패 시 포기하지 않고 최대 5초 동안 락이 풀리기를 기다리며 재시도합니다. (Pub/Sub 활용)
  - **Lease Time (10초)**: 락을 잡은 스레드에 에러가 나더라도 **10초 후 무조건 락이 풀리도록 설정**하여 데드락(Deadlock)을 방지합니다.

---

### [2026-07-13 15:35] Q10. 현업 개발자들의 API 테스트 방식과 도구
**질문 (User)**:
"실제 현업자들은 api테스트 어떤식으로해? 툴을 쓴다던지 SWAGGER를 쓴다던지 알려줘"

**답변 (Antigravity)**:
1. **Swagger / Springdoc OpenAPI**: 백엔드 자바 코드를 스캔하여 자동으로 API 명세 웹페이지(Swagger UI)를 만들고 인터랙티브 테스트를 제공합니다.
2. **Postman / Insomnia**: API 요청 환경(로컬, 운영)을 변수화하고 JWT 토큰을 자동 체이닝하여 테스트하는 데스크톱 툴입니다.
3. **JUnit + MockMvc / RestAssured**: 자바 코드로 자동화 테스트를 작성하여 서버 빌드 시점에 기존 기능의 무결성을 검증합니다.
4. **VS Code REST Client**: `.http` 텍스트 파일로 요청 템플릿을 만들어 버전 관리와 함께 가볍게 테스트합니다.

---

### [2026-07-13 15:40] Q11. Swagger를 이용한 백엔드 API 명세 자동화 및 실습 가이드
**질문 (User)**:
"swagger로 실습해보고 싶어 방법이랑 예제들 알려줘"

**답변 (Antigravity)**:
`build.gradle`에 `springdoc-openapi-starter-webmvc-ui:2.5.0` 의존성을 추가하고 `OpenApiConfig` Bean 및 컨트롤러에 `@Tag`, `@Operation` 어노테이션을 부여하여 `http://localhost:8080/swagger-ui/index.html`에 접속해 브라우저에서 직접 테스트할 수 있습니다.

---

### [2026-07-13 15:53] Q12. Swagger (OpenAPI 3) DTO @Schema 명세 및 @ApiResponse 응답 코드 고도화
**질문 (User)**:
"다시 swagger랑 api예제 구성해줘. DTO에 설명 추가하거나 응답 코드 설정하는 방법이랑 그 원리는 뭐야?"

**답변 (Antigravity)**:
1. **`@Schema`**: DTO 멤버 변수에 `description`, `example`, `requiredMode`를 부착하여 파라미터 양식과 견본 데이터를 예쁘게 가공합니다.
2. **`@ApiResponses` & `@ApiResponse`**: 성공(200 OK)뿐만 아니라 400 Bad Request, 409 Conflict 등의 예외 응답 시나리오를 명시하여 소통 비용을 0으로 줄입니다.

---

### [2026-07-13 15:58] Q13. API와 비즈니스 로직의 개념 차이 및 Swagger 실습 시나리오 가이드
**질문 (User)**:
"이제 화면은 나오는데 이걸로 어떤 걸 보고 테스트해야 하는지 모르겠어. 그리고 API가 로직이라고 생각하면 되는 걸까?"

**답변 (Antigravity)**:
* **API (주문 창구)**: 프론트엔드와 백엔드가 약속된 파라미터로 데이터를 주고받는 **통로/카운터**입니다.
* **비즈니스 로직 (주방 레시피)**: 백엔드 내부에서 검증, 연산, 락 획득, DB 저장을 수행하는 **실제 알고리즘**입니다.
* **5단계 실습 시나리오**: `1단계(가입)` ➔ `2단계(매장/좌석 등록)` ➔ `3단계(좌석 조회)` ➔ `4단계(5분 임시선점)` ➔ `5단계(결제확정)` 순서로 호출하여 테스트를 수행합니다.

---

### [2026-07-13 16:07] Q14. Swagger UI 결과(Response) 확인 위치 및 DB 외래키(Foreign Key) 제약 조건
**질문 (User)**:
"Execute를 누르면 결과에 네가 말한 내용(success: true 등)은 없고 curl -X 'POST' ... 이런 텍스트만 보이는데, 내 컴퓨터 터미널에서 따로 결과 값을 확인해야 하는 거야?"

**답변 (Antigravity)**:
cURL 영역 아래로 스크롤을 살짝 더 내리면 **`Responses` ➔ `Response body`** 상자에 진짜 결과 JSON 데이터가 출력됩니다. 만약 500 에러가 난다면 존재하지 않는 유저/좌석 ID로 접근하여 DB 외래키(FK) 무결성 조건이 깨진 것이므로 반드시 1단계 가입부터 순서대로 진행해야 합니다.

---

### [2026-07-13 16:15] Q15. 전역 예외 처리(RestControllerAdvice)와 정적 팩토리 메서드(Static Factory Method)의 이점
**질문 (User)**:
"백엔드 코드를 리팩토링하면서 전역 예외 처리기랑 from 같은 정적 메서드들을 적용했는데, 이 개념들이 자바/스프링에서 왜 중요하고 어떤 점이 좋은지 설명해줘."

**답변 (Antigravity)**:
* **`@RestControllerAdvice`**: 모든 컨트롤러의 예외를 공중에서 가로채 일관된 `ErrorResponse` JSON 객체로 반환하고, 예외 스택 노출을 막아 보안성을 높입니다.
* **정적 팩토리 메서드 (`from` 패턴)**: `UserDto.Response.from(user)`처럼 DTO가 스스로 엔티티 매핑 책임과 생성을 캡슐화하도록 만들어 서비스 레이어 코드가 압축되고 깔끔해집니다.

# 📚 ZariYo 학습 노트 - 2026년 7월 27일 (월)

본 문서는 2026년 7월 27일 진행된 메뉴·주문·직원호출 3대 코어 백엔드 도메인 구축, OpenStreetMap/Google Maps 지오코딩 실시간 지도 엔진, HTML5 Drag & Drop File Upload 및 손님 4단계 릴레이 UX 파이프라인 학습 기록입니다.

---

### [2026-07-27 12:28] Q16. 메뉴·주문·직원호출 백엔드 3대 코어 도메인(menu, order, staffcall) 구축 및 구조
**질문 (User)**:
"방금 백엔드 무슨 활동 한건지 알려줘"

**답변 (Antigravity)**:
프론트엔드의 키오스크/테이블 오더 및 사장님 실시간 관제 대시보드/KDS와 연동되는 핵심 3대 백엔드 도메인을 완성했습니다.
1. **메뉴 도메인 (`menu`)**: 카테고리(`Category`), 메뉴 항목(`MenuItem`), 커스텀 옵션(`MenuOption`) 엔티티 및 품절(Sold Out) 토글 API 구현.
2. **주문 도메인 (`order`)**: 주문 생성(`Order`), 주문 상세 항목(`OrderItem`), 상태 체인(`PENDING` ➔ `PREPARING` ➔ `SERVED` ➔ `COMPLETED`) 관리.
3. **직원 호출 도메인 (`staffcall`)**: 얼음물, 일회용 앞치마 등 손님의 편의 서비스 요청(`StaffCall`) 등록 및 처리 완료(`isResolved`) API 구현.

---

### [2026-07-27 12:30] Q17. 자리요(ZariYo) 실시간 주문-관제 상태 변화 데이터 비즈니스 로직
**질문 (User)**:
"지금 그러면 로직이 어떻게 되어있는거야"

**답변 (Antigravity)**:
* **손님 주문 시**: 손님이 키오스크 결제 시 REST API로 DB 저장 후 WebSocket STOMP로 사장님 대시보드에 알림이 수신됩니다.
* **주방 조리 ➔ 서빙 ➔ 퇴장**: 사장님이 KDS 버튼 클릭 시 `OrderStatus`가 변경되어 실시간 테이블 색상과 관제 상태가 동기화됩니다.

---

### [2026-07-27 12:48] Q18. 스프링 부트 서버 실행 로그 분석 (회원가입 이메일 중복 검증 예외 및 DB INSERT 쿼리)
**질문 (User)**:
"스프링 부트 로그 (Hibernate select, insert, GlobalExceptionHandler 경고 및 bootRun 80% executing) 분석 요청"

**답변 (Antigravity)**:
- `SELECT`: JPA가 이메일 중복 여부를 사전 검사.
- `GlobalExceptionHandler`: 중복 이메일 감지 시 전역 예외 처리기가 400 Bad Request 에러 반환.
- `INSERT`: 검증 통과 후 MySQL `users` 테이블에 영속 저장하고 JWT 토큰 발급.

---

### [2026-07-27 14:00] Q19. OpenStreetMap & Google Maps 지오코딩(Geocoding) 지점 실시간 맵핑 지도 API 작동 원리
**질문 (User)**:
"매장 등록할 때 주소를 입력하면 지도가 실시간으로 바뀌게 하고 싶어. 지오코딩(Geocoding)이란 무엇이고 어떤 수식으로 지도가 그려져?"

**답변 (Antigravity)**:
1. **지오코딩**: 주소 텍스트를 위경도 좌표 `[lat, lng]`로 변환하여 0.1초 만에 지도 카메라를 `map.flyTo([lat, lng], 16)` 수식으로 이동시킵니다.
2. **자동 폴백(Fallback)**: `.env`에 `VITE_GOOGLE_MAPS_API_KEY` 입력 시 Google Maps 엔진 가동, 키 미입력 시 CartoDB OpenStreetMap 엔진으로 자동 안전 전환됩니다.

---

### [2026-07-27 14:38] Q20. HTML5 Drag & Drop API와 FileReader를 활용한 이미지 파일 업로드 원리
**질문 (User)**:
"메뉴 이미지를 컴퓨터 탐색기에서 드래그 앤 드롭(Drag & Drop) 방식으로 파일 업로드하게 하고 싶은데, 웹 브라우저에서 파일 처리가 어떻게 이루어져?"

**답변 (Antigravity)**:
- `onDragOver` & `onDrop` 이벤트로 드래그 동작을 감지하고, `FileReader.readAsDataURL(file)` 메서드를 호출하여 이미지 파일 바이너리를 `data:image/png;base64,...` 문자열로 1초 만에 인코딩해 즉시 미리보기를 렌더링하고 저장합니다.

---

### [2026-07-27 14:45] Q21. 손님 회원가입 삭제 & 4단계 릴레이 순차 UX (전화번호 ➔ 매장검색 ➔ 메뉴창) 파이프라인
**질문 (User)**:
"손님한테 아이디 비밀번호 회원가입 시키는 건 번거로워. 전화번호 간단 입력 방문 로그와 매장 검색 순서로 릴레이 되는 UX 구조는 어떻게 설계해?"

**답변 (Antigravity)**:
손님 가입 절차를 지우고 `[1. 휴대폰 입력 KioskPhoneAuthModal]` ➔ `[2. 방문 매장 검색 KioskStoreSearchModal]` ➔ `[3. 2D 좌석선점 & 메뉴판]` ➔ `[4. 주문결제 & 직원호출]` 순차 릴레이 워크플로우를 완벽 연결했습니다. (QR 스캔 시 2단계는 자동 패스)
